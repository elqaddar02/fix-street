<div class="space-y-6">
    <div>
        <h1 class="text-3xl font-bold text-gray-900 mb-2">{{ __('help.authorities.title') }}</h1>
        <p class="text-gray-600">{{ __('help.authorities.subtitle') }}</p>
    </div>
    <div class="bg-white rounded-xl shadow-sm p-6">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">{{ __('help.authorities.managing_reports.title') }}</h2>
        <div class="space-y-4 text-gray-700">
            <p>{{ __('help.authorities.managing_reports.description1') }}</p>
            <p>{{ __('help.authorities.managing_reports.description2') }}</p>
            <p>{{ __('help.authorities.managing_reports.description3') }}</p>
        </div>
    </div>
</div>
